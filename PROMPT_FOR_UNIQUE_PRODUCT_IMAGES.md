# 🎨 Промпт: Генерация уникальных изображений для товаров каталога

## 📋 Контекст задачи

Я работаю над Next.js приложением - платформой для работы с китайскими поставщиками товаров.

**Текущая ситуация:**
- ✅ База данных Supabase с 459 товарами
- ✅ Товары загружаются и отображаются корректно
- ✅ Есть изображения, но они повторяются (каждое фото используется ~2 раза)

**Проблема:**
Нужно чтобы у **первых 50 товаров** были **УНИКАЛЬНЫЕ** изображения, подобранные **индивидуально под каждый товар** с учетом:
- Названия товара
- Категории
- Описания
- Специфики товара

---

## 🎯 Задача

Создать скрипт который:

1. **Загружает первые 50 товаров** из Supabase
2. **Для КАЖДОГО товара** находит релевантное изображение из Unsplash
3. **Использует Unsplash API** для поиска по ключевым словам
4. **Обновляет изображения** в базе данных

---

## 🔑 Доступы

### Supabase
```javascript
const supabaseUrl = 'https://rbngpxwamfkunktxjtqh.supabase.co'
const supabaseAnonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InJibmdweHdhbWZrdW5rdHhqdHFoIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDg1OTk5NDcsImV4cCI6MjA2NDE3NTk0N30.cpW1S5MK7eOfYSZx9gHP_AP-wH5BRIigUFwlBYNA2MI'
```

### Unsplash API (нужно получить)
```
1. Зарегистрируйтесь на https://unsplash.com/developers
2. Создайте приложение
3. Получите Access Key
```

---

## 📊 Структура товара в базе

```typescript
interface Product {
  id: string              // UUID
  name: string           // "Смартфон Xiaomi 13 Pro"
  category_id: string    // UUID категории
  description: string    // "Флагманский смартфон..."
  price: number          // 59500
  images: string[]       // URL изображений
  sku: string            // Артикул
  min_order: number      // MOQ
  in_stock: boolean      // Наличие
  supplier_id: string    // UUID поставщика
}
```

---

## 🔧 Технический план

### Шаг 1: Установить Unsplash SDK
```bash
npm install unsplash-js
```

### Шаг 2: Создать скрипт `scripts/assign-contextual-images.js`

**Алгоритм:**
```javascript
1. Загрузить первые 50 товаров из Supabase
2. Для каждого товара:
   a. Извлечь ключевые слова из названия (убрать артикулы, модели)
   b. Добавить категорию к запросу
   c. Использовать Unsplash API для поиска изображения
   d. Взять первое релевантное фото
   e. Обновить товар в Supabase
3. Добавить задержку между запросами (Unsplash rate limit)
4. Логировать прогресс
```

---

## 📝 Пример кода

### Извлечение ключевых слов
```javascript
function extractKeywords(productName) {
  // Убираем артикулы, цифры, модели
  let keywords = productName
    .replace(/\d+/g, '')           // Убрать цифры
    .replace(/[A-Z]{2,}/g, '')     // Убрать артикулы (2+ заглавные)
    .replace(/Pro|Max|Ultra|Plus/gi, '') // Убрать модификаторы
    .trim()

  return keywords
}

// Примеры:
extractKeywords("Смартфон Xiaomi 13 Pro")  // → "Смартфон Xiaomi"
extractKeywords("Ноутбук HP 66T")          // → "Ноутбук"
extractKeywords("Наушники Baseus Ultra")   // → "Наушники Baseus"
```

### Поиск изображения на Unsplash
```javascript
async function findImageForProduct(product, categoryName) {
  const keywords = extractKeywords(product.name)
  const query = `${keywords} ${categoryName}` // "Смартфон Xiaomi Электроника"

  try {
    const result = await unsplash.search.getPhotos({
      query: query,
      perPage: 1,
      orientation: 'squarish' // Квадратные изображения
    })

    if (result.response.results.length > 0) {
      const photo = result.response.results[0]
      return photo.urls.regular // URL изображения
    }
  } catch (error) {
    console.error(`Ошибка поиска для ${product.name}:`, error)
    return null
  }
}
```

### Обновление в Supabase
```javascript
async function updateProductImage(productId, imageUrl) {
  const { error } = await supabase
    .from('products')
    .update({ images: [imageUrl] })
    .eq('id', productId)

  if (error) {
    console.error('Ошибка обновления:', error)
    return false
  }
  return true
}
```

---

## 🎯 Примеры товаров и ожидаемые результаты

### Товар 1: "Смартфон Xiaomi 13 Pro"
- **Категория:** Электроника
- **Ключевые слова:** "Смартфон Xiaomi"
- **Unsplash запрос:** "Smartphone Xiaomi Electronics"
- **Ожидаемое фото:** Реальное фото Xiaomi смартфона

### Товар 2: "Диван угловой тканевый"
- **Категория:** Мебель
- **Ключевые слова:** "Диван угловой тканевый"
- **Unsplash запрос:** "Corner fabric sofa furniture"
- **Ожидаемое фото:** Фото углового дивана

### Товар 3: "Куртка мужская демисезонная"
- **Категория:** Одежда
- **Ключевые слова:** "Куртка мужская демисезонная"
- **Unsplash запрос:** "Men jacket clothing"
- **Ожидаемое фото:** Фото мужской куртки

---

## ⚠️ Важные моменты

### Rate Limits Unsplash
- **Demo mode:** 50 запросов в час
- **Production:** 5000 запросов в час
- **Решение:** Добавить задержку 2-3 секунды между запросами

```javascript
// Задержка между запросами
await new Promise(resolve => setTimeout(resolve, 3000))
```

### Обработка ошибок
```javascript
// Если изображение не найдено - пропустить товар
if (!imageUrl) {
  console.warn(`⚠️  Изображение не найдено для: ${product.name}`)
  continue
}
```

### Логирование прогресса
```javascript
console.log(`\n[${index + 1}/50] Обрабатываем: ${product.name}`)
console.log(`   Категория: ${categoryName}`)
console.log(`   Запрос: "${searchQuery}"`)
console.log(`   ✅ Найдено изображение!`)
```

---

## 📦 Структура скрипта

```javascript
// scripts/assign-contextual-images.js

const { createApi } = require('unsplash-js')
const { createClient } = require('@supabase/supabase-js')

// 1. Настройка клиентов
const unsplash = createApi({ accessKey: process.env.UNSPLASH_ACCESS_KEY })
const supabase = createClient(supabaseUrl, supabaseAnonKey)

// 2. Вспомогательные функции
function extractKeywords(productName) { ... }
async function findImageForProduct(product, categoryName) { ... }
async function updateProductImage(productId, imageUrl) { ... }

// 3. Главная функция
async function assignContextualImages() {
  console.log('🎨 Назначаем контекстные изображения для первых 50 товаров...\n')

  // Загружаем категории
  const { data: categories } = await supabase
    .from('categories')
    .select('*')

  const categoryMap = new Map(categories.map(c => [c.id, c.name]))

  // Загружаем первые 50 товаров
  const { data: products } = await supabase
    .from('products')
    .select('*')
    .limit(50)

  console.log(`✅ Найдено товаров: ${products.length}\n`)

  // Обрабатываем каждый товар
  for (let i = 0; i < products.length; i++) {
    const product = products[i]
    const categoryName = categoryMap.get(product.category_id)

    console.log(`\n[${i + 1}/50] ${product.name}`)

    // Находим изображение
    const imageUrl = await findImageForProduct(product, categoryName)

    if (imageUrl) {
      // Обновляем в базе
      await updateProductImage(product.id, imageUrl)
      console.log(`   ✅ Обновлено!`)
    } else {
      console.log(`   ⚠️  Пропущено (изображение не найдено)`)
    }

    // Задержка для rate limit
    await new Promise(resolve => setTimeout(resolve, 3000))
  }

  console.log('\n✅ ГОТОВО!')
}

// Запуск
assignContextualImages()
```

---

## 🚀 Запуск скрипта

### 1. Получить Unsplash Access Key
```
https://unsplash.com/oauth/applications
```

### 2. Добавить в .env.local
```bash
UNSPLASH_ACCESS_KEY=your_access_key_here
```

### 3. Запустить скрипт
```bash
node scripts/assign-contextual-images.js
```

---

## ✅ Ожидаемый результат

После выполнения скрипта:

1. **Первые 50 товаров** будут иметь **уникальные** изображения
2. Каждое изображение **релевантно** конкретному товару
3. Изображения **высокого качества** от Unsplash
4. **Нет повторов** между товарами

### Примеры до/после:

**ДО:**
- Товар 1: generic electronics image
- Товар 2: generic electronics image (повтор)
- Товар 3: generic electronics image (повтор)

**ПОСЛЕ:**
- Товар 1 "Xiaomi 13 Pro": реальное фото Xiaomi
- Товар 2 "iPhone 15": реальное фото iPhone
- Товар 3 "Samsung Galaxy": реальное фото Samsung

---

## 🔄 Альтернативный подход (если Unsplash не работает)

### Использовать Pexels API
```javascript
const { createClient } = require('pexels')
const pexels = createClient(process.env.PEXELS_API_KEY)

const photos = await pexels.photos.search({
  query: searchQuery,
  per_page: 1
})
```

### Использовать Pixabay API
```javascript
const response = await fetch(
  `https://pixabay.com/api/?key=${process.env.PIXABAY_API_KEY}&q=${searchQuery}&per_page=1`
)
```

---

## 📊 Метрики успеха

После выполнения проверь:

```sql
-- Количество уникальных изображений в первых 50
SELECT COUNT(DISTINCT images)
FROM products
LIMIT 50;
-- Должно быть: 50 (или близко к 50)

-- Товары без изображений
SELECT name
FROM products
WHERE images IS NULL OR images = '[]'
LIMIT 50;
-- Должно быть: 0
```

---

## 💡 Советы по оптимизации

### 1. Кэширование результатов
```javascript
// Сохранить результаты в JSON
const results = []
// После обработки
fs.writeFileSync('image-results.json', JSON.stringify(results, null, 2))
```

### 2. Retry логика
```javascript
async function findImageWithRetry(product, categoryName, retries = 3) {
  for (let i = 0; i < retries; i++) {
    try {
      return await findImageForProduct(product, categoryName)
    } catch (error) {
      if (i === retries - 1) throw error
      await new Promise(resolve => setTimeout(resolve, 5000))
    }
  }
}
```

### 3. Fallback на generic изображения
```javascript
if (!imageUrl) {
  // Использовать generic изображение для категории
  imageUrl = GENERIC_IMAGES[categoryName]
}
```

---

## 📝 Чеклист выполнения

- [ ] Зарегистрироваться на Unsplash Developers
- [ ] Получить Access Key
- [ ] Добавить в .env.local
- [ ] Установить `unsplash-js`: `npm install unsplash-js`
- [ ] Создать скрипт `scripts/assign-contextual-images.js`
- [ ] Реализовать функцию extractKeywords
- [ ] Реализовать поиск на Unsplash
- [ ] Добавить обновление в Supabase
- [ ] Добавить обработку ошибок
- [ ] Добавить rate limiting (задержки)
- [ ] Запустить скрипт
- [ ] Проверить результаты в базе
- [ ] Открыть http://localhost:3000/catalog
- [ ] Убедиться что у первых 50 товаров уникальные фото

---

## 🎉 Финальная проверка

После выполнения скрипта:

1. **Перезагрузи страницу** http://localhost:3000/catalog
2. **Прокрути первые 50 товаров**
3. **Убедись что:**
   - ✅ Каждый товар имеет своё уникальное изображение
   - ✅ Изображения релевантны товарам
   - ✅ Нет повторов
   - ✅ Качество изображений высокое

---

## 🆘 Если что-то пошло не так

### Проблема: Rate limit exceeded
**Решение:** Увеличь задержку между запросами до 5-10 секунд

### Проблема: Изображения не найдены
**Решение:** Упрости поисковые запросы, используй английские термины

### Проблема: Ошибка обновления Supabase
**Решение:** Проверь RLS политики, используй service_role key если нужно

---

## 📞 Дополнительная информация

**Текущая рабочая директория:** `/Users/user/Downloads/code`

**База данных:** Supabase (PostgreSQL)

**Таблица товаров:** `products`

**Текущий dev сервер:** http://localhost:3000 (порт 3000)

---

## 🎯 Цель задачи

Сделать так чтобы у **первых 50 товаров** каталога были **уникальные релевантные изображения**, подобранные индивидуально под каждый товар с использованием Unsplash API.

**Успех = 50 товаров, 50 уникальных релевантных фото!**
