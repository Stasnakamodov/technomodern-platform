# 📦 Руководство по генерации каталога товаров

## 🎯 Что было сделано

Создан автоматический генератор **459 реалистичных товаров** для MVP платформы ТехноМодерн.

### ✨ Особенности каталога:

- **8 категорий** (Электроника, Одежда, Мебель, Строительство, Автотовары, Дом и сад, Спорт, Красота)
- **48 подкатегорий**
- **8 китайских поставщиков** с рейтингами
- **Реалистичные цены** с расчетом маржи (1688 → Ozon/WB)
- **Описания на русском**
- **Изображения** (Unsplash URLs)
- **Характеристики** товаров

---

## 📊 Статистика каталога

```
💻 Электроника:        76 товаров
👕 Одежда:             75 товаров
🪑 Мебель:             60 товаров
🔨 Строительство:      48 товаров
🚗 Автотовары:         49 товаров
🏠 Дом и сад:          57 товаров
⚽ Спорт и отдых:      45 товаров
💄 Красота и здоровье: 49 товаров
────────────────────────────────
   ИТОГО:             459 товаров
```

---

## 🚀 Как использовать

### 1. Генерация нового каталога

```bash
# Генерировать новый каталог (459 товаров)
npx tsx scripts/generate-catalog.ts
```

Результат: файл `data/realistic-catalog-v2.json`

### 2. Импорт в базу данных

```bash
# Создать SQL миграцию
npx tsx scripts/import-catalog-to-db.ts --sql-only
```

Результат: файл `supabase/migrations/002_import_catalog_data.sql`

### 3. Применение в Supabase

**Вариант А: Supabase CLI** (если настроен)
```bash
npx supabase db push
```

**Вариант Б: Вручную через Dashboard**
1. Откройте Supabase Dashboard → SQL Editor
2. Скопируйте содержимое `002_import_catalog_data.sql`
3. Выполните запрос
4. Проверьте результат

---

## 📋 Структура данных

### Товар (Product)
```json
{
  "id": "prod-0001",
  "name": "Смартфон POCO Pro",
  "category": "Электроника",
  "subcategory": "Смартфоны",
  "description": "Качественный смартфон...",
  "price_1688_cny": 4614,
  "price_rub": 80376,
  "margin_percent": 34,
  "supplier": "Dongguan Industrial",
  "supplier_city": "Дунгуань",
  "supplier_verified": true,
  "supplier_rating": 4.8,
  "moq": 1,
  "in_stock": true,
  "image_url": "https://images.unsplash.com/...",
  "specifications": {
    "spec_1": "8GB RAM",
    "spec_2": "AMOLED",
    "spec_3": "120Hz"
  }
}
```

### Поставщик (Supplier)
```json
{
  "name": "Guangzhou Tech Co.",
  "country": "Китай",
  "city": "Гуанчжоу",
  "verified": true,
  "rating": 4.8
}
```

---

## 🔧 Настройка генератора

Файл: `scripts/generate-catalog.ts`

### Добавить категорию:
```typescript
{
  id: 'new_category',
  name: 'Новая категория',
  icon: '📱',
  subcategories: ['Подкатегория 1', 'Подкатегория 2']
}
```

### Добавить шаблоны товаров:
```typescript
new_category: [
  {
    namePattern: 'Товар {brand} {model}',
    brands: ['Brand1', 'Brand2'],
    price1688Range: [100, 500],
    marginPercent: [30, 80],
    moqRange: [10, 50],
    keywords: ['Ключевое слово 1', 'Ключевое слово 2']
  }
]
```

### Изменить количество товаров:
```typescript
// В функции generateProducts()
const productsCount = randomFromRange(8, 12) // измените диапазон
```

---

## 💡 Примеры маржи

Из вашей таблицы:

| Товар | Цена 1688 (CNY) | Цена Ozon/WB (RUB) | Маржа |
|-------|----------------|-------------------|-------|
| iPhone 15 Pro Max | 6000-7000 | 80000-95000 | 20-30% |
| MacBook Pro M3 | 16000-19000 | 220000-280000 | 25-35% |
| Xiaomi 13 Ultra | 4500-5500 | 55000-70000 | 20-30% |
| Nike Air Max | 350-500 | 5000-7000 | 30-50% |
| Футболка хлопок | 30-50 | 400-800 | 50-100% |
| LED лента RGB | 60-100 | 800-1500 | 40-80% |

---

## 🎨 Фотографии товаров

### Текущие: Unsplash placeholders
```
https://images.unsplash.com/photo-{timestamp}?...
```

### Для production:

1. **Замените на реальные фото:**
   - Скачайте с Unsplash API (бесплатно 50 запросов/час)
   - Или используйте парсинг AliExpress/1688

2. **Unsplash API:**
```typescript
const response = await fetch(
  `https://api.unsplash.com/photos/random?query=${category}&client_id=YOUR_KEY`
)
```

3. **Загрузите в Supabase Storage:**
```typescript
const { data } = await supabase.storage
  .from('product-images')
  .upload(`products/${product.id}.jpg`, imageFile)
```

---

## 🔍 Проверка данных

### SQL запросы для проверки:

```sql
-- Всего товаров
SELECT COUNT(*) FROM products;

-- Товары по категориям
SELECT c.name, COUNT(p.id) as total
FROM categories c
LEFT JOIN products p ON p.category_id = c.id
WHERE c.level = 1
GROUP BY c.name;

-- Поставщики с количеством товаров
SELECT s.name, COUNT(p.id) as products_count
FROM suppliers s
LEFT JOIN products p ON p.supplier_id = s.id
GROUP BY s.name;

-- Средняя цена по категориям
SELECT c.name,
       ROUND(AVG(p.price)) as avg_price,
       MIN(p.price) as min_price,
       MAX(p.price) as max_price
FROM categories c
JOIN products p ON p.category_id = c.id
WHERE c.level = 1
GROUP BY c.name;
```

---

## 📝 TODO / Улучшения

- [ ] Добавить больше товаров (до 1000+)
- [ ] Загрузить реальные фото с Unsplash API
- [ ] Добавить отзывы и рейтинги товаров
- [ ] Создать связи между товарами (похожие товары)
- [ ] Добавить вариации товаров (цвета, размеры)
- [ ] Импортировать реальные данные через парсер AliExpress

---

## 🆘 Проблемы и решения

### Ошибка: "Duplicate key value"
```sql
-- Очистите таблицы перед повторным импортом
TRUNCATE TABLE products CASCADE;
TRUNCATE TABLE suppliers CASCADE;
TRUNCATE TABLE categories CASCADE;
```

### Ошибка: "Foreign key violation"
```sql
-- Проверьте, что поставщики и категории созданы до товаров
SELECT * FROM suppliers;
SELECT * FROM categories;
```

### Слишком долгий импорт
- Разбейте SQL на несколько файлов по 100-150 товаров
- Используйте batch insert вместо отдельных INSERT

---

## 📚 Дополнительные ресурсы

- [Unsplash API](https://unsplash.com/developers)
- [1688.com](https://www.1688.com) - оригинальные цены
- [Ozon Seller](https://seller.ozon.ru) - актуальные цены на маркетплейсах
- [Wildberries Analytics](https://www.wildberries.ru) - аналитика продаж

---

**Создано:** ${new Date().toISOString()}
**Версия:** 1.0.0
