# 🚨 Устранение проблем с каталогом

**Дата:** 2025-11-12
**Проблема:** Товары не отображаются на странице каталога

---

## ✅ Что уже сделано

### 1. Данные в Supabase
- ✅ **459 товаров** загружены и проверены
- ✅ **8 поставщиков** с информацией
- ✅ **53 категории** для организации
- ✅ Все изображения обновлены (Unsplash Collections)
- ✅ RLS политики настроены корректно

### 2. Тестовые скрипты
```bash
# Все тесты проходят успешно:
npx tsx scripts/test-catalog-load.ts       # ✅
npx tsx scripts/check-rls-policies.ts      # ✅
```

### 3. Улучшения кода
- ✅ Добавлено детальное логирование в консоль
- ✅ Добавлена проверка на null/undefined
- ✅ Исправлен формат цены (₽ вместо $)
- ✅ Добавлена валидация данных в ProductCard

---

## 🔧 Новые инструменты для отладки

### Тестовая страница каталога
**URL:** http://localhost:3000/catalog-test

Эта страница:
- Загружает 10 товаров из Supabase
- Показывает подробный статус загрузки
- Отображает данные в простом формате
- Выводит ошибки если они есть

**Как использовать:**
1. Запустите `npm run dev`
2. Откройте http://localhost:3000/catalog-test
3. Посмотрите что отображается
4. Откройте консоль браузера (F12) для логов

---

## 📋 Пошаговая диагностика

### Шаг 1: Проверьте что сервер запущен
```bash
npm run dev
```

Должно быть:
```
✓ Ready in 2.3s
○ Local:   http://localhost:3000
```

### Шаг 2: Откройте тестовую страницу
```
http://localhost:3000/catalog-test
```

**Ожидаемый результат:**
```
✅ Загружено 10 товаров
```

Если видите это - **Supabase работает**, проблема в основном каталоге.

Если видите ошибку - **проблема с подключением к Supabase**.

### Шаг 3: Откройте основной каталог
```
http://localhost:3000/catalog
```

### Шаг 4: Откройте консоль браузера

**Chrome/Brave/Edge:** `Cmd+Option+I` (Mac) или `F12` (Windows)

**Safari:** `Cmd+Option+C` (включите Developer menu в настройках)

### Шаг 5: Проверьте логи

**Если видите:**
```
🔄 Начинаем загрузку товаров...
📦 Загружаем товары из Supabase...
📦 Ответ от Supabase (товары): {dataLength: 459, error: null, hasData: true}
🏭 Загружено поставщиков: 8
📂 Загружено категорий: 53
✅ Трансформировано 459 товаров
✅ Загрузка завершена, loading = false
```

Значит **данные загружаются**, но есть проблема с рендерингом.

**Если видите ошибки** - скопируйте их и отправьте.

---

## 🐛 Типичные проблемы и решения

### Проблема 1: "Cannot read property 'map' of null"
**Причина:** Данные не загрузились

**Решение:**
```bash
# Переимпортируйте данные
npx tsx scripts/import-to-supabase.ts
```

### Проблема 2: "Network Error" или "Failed to fetch"
**Причина:** CORS или firewall

**Решение:**
1. Проверьте интернет соединение
2. Отключите VPN/proxy
3. Попробуйте другой браузер
4. Проверьте что Supabase доступен:
   ```bash
   curl https://rbngpxwamfkunktxjtqh.supabase.co/rest/v1/
   ```

### Проблема 3: Страница зависает на "Загружаем товары"
**Причина:** Не срабатывает `setLoading(false)`

**Что делать:**
1. Откройте консоль
2. Найдите последнее сообщение
3. Если видите `✅ Загрузка завершена` - проблема в рендеринге
4. Если нет - проблема в загрузке данных

### Проблема 4: "Товары не найдены" хотя они есть
**Причина:** Фильтры или поиск

**Решение:**
1. Очистите поле поиска
2. Нажмите "Все товары" в категориях
3. Обновите страницу (Cmd+R / Ctrl+R)

### Проблема 5: Изображения не загружаются
**Причина:** Unsplash Source API не работает

**Это нормально**, товары должны отображаться с иконкой-заглушкой.

---

## 📸 Что нужно проверить

Откройте Developer Tools и проверьте:

### 1. Console (Консоль)
Должны быть логи:
- 🔄 Начинаем загрузку товаров...
- ✅ Загружено 459 товаров

**Если есть ошибки (красный текст)** - скопируйте их полностью.

### 2. Network (Сеть)
Найдите запросы к:
- `rbngpxwamfkunktxjtqh.supabase.co`

Должны быть статусы:
- ✅ 200 OK

**Если видите:**
- ❌ 401 Unauthorized - проблема с API ключом
- ❌ 403 Forbidden - проблема с RLS политиками
- ❌ 500 Internal Server Error - проблема на сервере Supabase

### 3. Elements (Элементы)
Найдите:
```html
<div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-8">
```

**Если этого div нет** - товары не рендерятся.

**Если div есть, но внутри пусто** - ProductCard падает с ошибкой.

---

## 🔍 Детальная диагностика

### Запустите в консоли браузера:

```javascript
// Проверка что Supabase клиент доступен
console.log('Supabase:', window.supabase)

// Проверка что можно загрузить данные
fetch('https://rbngpxwamfkunktxjtqh.supabase.co/rest/v1/products?select=*&limit=1', {
  headers: {
    'apikey': 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InJibmdweHdhbWZrdW5rdHhqdHFoIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDg1OTk5NDcsImV4cCI6MjA2NDE3NTk0N30.cpW1S5MK7eOfYSZx9gHP_AP-wH5BRIigUFwlBYNA2MI'
  }
})
.then(r => r.json())
.then(data => console.log('✅ Данные:', data))
.catch(err => console.error('❌ Ошибка:', err))
```

**Ожидаемый результат:**
```
✅ Данные: [{id: '...', name: '...', price: ...}]
```

---

## 📝 Что делать дальше

### Если тестовая страница работает, а основная нет:

**Проблема в компонентах:**
1. `app/catalog/components/ProductCard.tsx`
2. `app/catalog/components/CategoryBrowser.tsx`

**Действия:**
1. Откройте консоль на странице /catalog
2. Найдите ошибки с `❌ ProductCard:`
3. Проверьте что все поля товара заполнены

### Если обе страницы не работают:

**Проблема с Supabase подключением:**
1. Проверьте файл `lib/supabase.ts`
2. Проверьте что URL и ключ корректны
3. Попробуйте в другом браузере
4. Проверьте интернет соединение

### Если видите товары, но они странно выглядят:

**Проблема с CSS:**
1. Убедитесь что Tailwind загружен
2. Проверьте что нет ошибок в стилях
3. Попробуйте очистить кеш браузера

---

## 🚀 Быстрый перезапуск

Если ничего не помогает:

```bash
# 1. Остановите сервер (Ctrl+C)

# 2. Очистите кеш Next.js
rm -rf .next

# 3. Переустановите зависимости (опционально)
rm -rf node_modules
npm install

# 4. Перезапустите сервер
npm run dev

# 5. Откройте в приватном окне браузера
# Chrome: Cmd+Shift+N / Ctrl+Shift+N
```

---

## 📞 Что отправить для помощи

1. **Скриншот консоли браузера** (весь вывод)
2. **Скриншот Network tab** (запросы к Supabase)
3. **Что видите на странице /catalog-test**
4. **Что видите на странице /catalog**
5. **Версия браузера** (Chrome 120, Safari 17, etc.)

---

## 🎯 Итоговый чеклист

- [ ] Сервер запущен: `npm run dev`
- [ ] Открыл /catalog-test - **работает** ✅ / **не работает** ❌
- [ ] Открыл /catalog - **работает** ✅ / **не работает** ❌
- [ ] Консоль браузера открыта (F12)
- [ ] Вижу логи `🔄 Начинаем загрузку...`
- [ ] Вижу логи `✅ Загружено ... товаров`
- [ ] Есть ошибки в консоли: **да** / **нет**
- [ ] Network tab показывает запросы к Supabase: **да** / **нет**
- [ ] Статус запросов: 200 OK / другой

---

**Создано:** 2025-11-12
**Статус:** Готово к тестированию
**Следующий шаг:** Откройте /catalog-test и сообщите результат
