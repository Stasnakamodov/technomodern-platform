# 🔧 Промпт: Исправление блокеров деплоя

## 📋 Контекст

Проект: **Next.js 15 платформа для работы с китайскими поставщиками**
Рабочая директория: `/Users/user/Downloads/code`

**Что произошло:**
- ✅ Проведен полный pre-deployment аудит
- ✅ Найдено **5 критических блокеров** деплоя
- ✅ Найдено **10 warnings** (не блокируют, но нужно исправить)
- ❌ Проект **НЕ ГОТОВ** к деплою

**Отчет аудита:** `/Users/user/Downloads/code/PRE_DEPLOYMENT_AUDIT_REPORT.md`

---

## 🎯 ТВОЯ ЗАДАЧА

1. **Прочитай отчет аудита** - `PRE_DEPLOYMENT_AUDIT_REPORT.md`
2. **Создай детальный план** исправления всех 5 критических блокеров
3. **Подготовь код** для каждого исправления
4. **Создай checklist** для проверки после исправлений
5. **Напиши команды** для валидации фиксов

---

## 📖 ШАГ 1: ПРОЧИТАЙ ОТЧЕТ

Прочитай файл `/Users/user/Downloads/code/PRE_DEPLOYMENT_AUDIT_REPORT.md` полностью.

Обрати внимание на секцию **"❌ BLOCKERS (НЕЛЬЗЯ ДЕПЛОИТЬ)"** - там 5 критических проблем.

---

## 📝 ШАГ 2: СОЗДАЙ ПЛАН ИСПРАВЛЕНИЙ

Для **каждого из 5 блокеров** создай детальный план:

### Формат плана:

```markdown
## BLOCKER #1: [Название проблемы]

### Описание проблемы:
[Что не так]

### Файлы затронуты:
- `/путь/к/файлу.ts:строка`

### Текущий код:
```typescript
// текущий проблемный код
```

### Исправленный код:
```typescript
// исправленный код с комментариями
```

### Шаги исправления:
1. [Конкретный шаг 1]
2. [Конкретный шаг 2]
...

### Команды для проверки:
```bash
# команды для валидации что фикс работает
```

### Риски:
[Что может сломаться после этого фикса]
```

---

## 🔧 ШАГ 3: ПОДГОТОВЬ КОД ДЛЯ КАЖДОГО БЛОКЕРА

### BLOCKER #1: Хардкод Supabase Credentials

**Что нужно:**
1. Создать `.env.local` с переменными
2. Обновить `lib/supabase.ts` для использования env
3. Добавить `.env.example` для команды
4. Обновить `.gitignore` чтобы не коммитить `.env.local`

**Подготовь:**
- Полный код `.env.local`
- Обновленный `lib/supabase.ts`
- Проверочные команды

---

### BLOCKER #2: TypeScript JSX.Element Error

**Что нужно:**
1. Найти все использования `JSX.Element`
2. Заменить на `ReactElement`
3. Добавить импорт `import type { ReactElement } from 'react'`

**Подготовь:**
- Команду поиска всех JSX.Element
- Исправленный код
- Команду проверки TypeScript

---

### BLOCKER #3: Placeholder Контакты

**Что нужно:**
1. Заменить placeholder ссылки на env переменные или реальные контакты
2. Обновить `components/header.tsx`

**Подготовь:**
- Обновленный код header.tsx
- Env переменные для контактов

---

### BLOCKER #4: Отсутствие Error Boundary

**Что нужно:**
1. Создать `app/error.tsx` (global error boundary)
2. Создать `app/catalog/error.tsx` (catalog-specific)
3. Добавить Suspense boundaries где нужно

**Подготовь:**
- Полный код для `app/error.tsx`
- Полный код для `app/catalog/error.tsx`
- Опционально: `app/loading.tsx`

---

### BLOCKER #5: Отсутствие try-catch в Server Component

**Что нужно:**
1. Обернуть `getCatalogStats()` в try-catch
2. Добавить fallback значения при ошибке
3. Логировать ошибки правильно

**Подготовь:**
- Исправленный код `components/catalog-section.tsx`
- Показать где добавить try-catch

---

## ✅ ШАГ 4: СОЗДАЙ CHECKLIST ДЛЯ ВАЛИДАЦИИ

После всех исправлений создай чеклист:

```markdown
## 🔍 Checklist валидации фиксов

### TypeScript
- [ ] `npx tsc --noEmit` - 0 ошибок
- [ ] Все импорты корректны
- [ ] Нет использования `any` в новом коде

### Environment Variables
- [ ] `.env.local` создан и заполнен
- [ ] `.env.example` создан для команды
- [ ] `.gitignore` содержит `.env.local`
- [ ] Все env переменные используются через `process.env`

### Error Handling
- [ ] `app/error.tsx` создан
- [ ] `app/catalog/error.tsx` создан
- [ ] try-catch добавлены в Server Components
- [ ] Ошибки логируются правильно

### Контакты
- [ ] Header содержит реальные контакты или env переменные
- [ ] Нет placeholder ссылок

### Build
- [ ] `npm run build` - успешно
- [ ] Нет warnings в консоли
- [ ] `npm run start` - приложение запускается

### Runtime Tests
- [ ] Главная страница загружается
- [ ] Каталог работает
- [ ] Нет ошибок в браузерной консоли
- [ ] Supabase подключение работает
```

---

## 🚀 ШАГ 5: КОМАНДЫ ДЛЯ ПРИМЕНЕНИЯ ФИКСОВ

Создай bash скрипт `fix-blockers.sh` который:

1. Делает backup текущих файлов
2. Применяет все исправления
3. Запускает проверки
4. Выводит результат

```bash
#!/bin/bash

echo "🔧 Starting deployment blocker fixes..."

# Backup
echo "📦 Creating backup..."
mkdir -p .backup
cp lib/supabase.ts .backup/
cp components/catalog-section.tsx .backup/
cp components/header.tsx .backup/

# Apply fixes
echo "✨ Applying fixes..."
# [команды для применения фиксов]

# Validate
echo "🔍 Validating fixes..."
npx tsc --noEmit
if [ $? -eq 0 ]; then
  echo "✅ TypeScript check passed"
else
  echo "❌ TypeScript check failed"
  exit 1
fi

npm run build
if [ $? -eq 0 ]; then
  echo "✅ Build successful"
else
  echo "❌ Build failed"
  exit 1
fi

echo "🎉 All fixes applied and validated!"
```

---

## 📊 ШАГ 6: ФИНАЛЬНЫЙ ОТЧЕТ

После создания плана, выведи краткую сводку:

```markdown
# Резюме Плана Исправлений

## Файлы для изменения:
1. `lib/supabase.ts` - env variables
2. `components/catalog-section.tsx` - try-catch + JSX.Element
3. `components/header.tsx` - контакты
4. `app/error.tsx` - новый файл
5. `app/catalog/error.tsx` - новый файл
6. `.env.local` - новый файл
7. `.env.example` - новый файл

## Время на исправление:
- Blocker #1: 5 минут
- Blocker #2: 3 минуты
- Blocker #3: 2 минуты
- Blocker #4: 10 минут
- Blocker #5: 5 минут
**Итого: ~25-30 минут**

## Порядок исправлений:
1. Сначала #2 (TypeScript) - самый простой
2. Потом #1 (env) - критичный для безопасности
3. Затем #5 (try-catch) - связан с #2
4. Потом #4 (error boundaries) - инфраструктура
5. Наконец #3 (контакты) - UX

## Команды после исправлений:
```bash
npx tsc --noEmit  # проверка типов
npm run build     # проверка билда
npm run start     # проверка runtime
```
```

---

## 🎯 КРИТЕРИИ УСПЕХА

План считается готовым когда:

✅ Для каждого блокера есть:
- Описание проблемы
- Текущий код
- Исправленный код
- Шаги применения
- Команды проверки

✅ Создан checklist валидации

✅ Подготовлен bash скрипт для применения

✅ Указано время на исправления

✅ Определен порядок исправлений

---

## ⚡ БЫСТРЫЙ СТАРТ

Просто скопируй это в новый чат:

```
Привет! Мне нужна твоя помощь с исправлением критических блокеров деплоя.

Контекст:
- Проект: Next.js 15 платформа
- Директория: /Users/user/Downloads/code
- Аудит завершен, найдено 5 критических блокеров

Задача:
1. Прочитай отчет: /Users/user/Downloads/code/PRE_DEPLOYMENT_AUDIT_REPORT.md
2. Изучи секцию "❌ BLOCKERS (НЕЛЬЗЯ ДЕПЛОИТЬ)"
3. Создай детальный план исправления всех 5 блокеров
4. Для каждого блокера подготовь:
   - Описание проблемы
   - Текущий код
   - Исправленный код с комментариями
   - Шаги применения
   - Команды проверки
5. Создай checklist валидации
6. Подготовь bash скрипт fix-blockers.sh для применения всех фиксов

Требования к плану:
- Максимально детальный
- Готовый к копированию код
- Указать риски каждого изменения
- Определить порядок исправлений
- Указать время на каждый фикс

Начни с чтения отчета и создания плана для Blocker #1.
```

---

## 📌 ВАЖНЫЕ ЗАМЕЧАНИЯ

- **НЕ ПРОПУСКАЙ** ни один блокер
- **НЕ УПРОЩАЙ** код - давай полные решения
- **ПРОВЕРЯЙ** что код компилируется
- **ДУМАЙ О РИСКАХ** - что может сломаться
- **ТЕСТИРУЙ ВСЕ** после применения

---

## 🔥 ПРИОРИТЕТЫ

**Сначала исправь что блокирует билд:**
1. Blocker #2 (TypeScript)

**Потом что критично для безопасности:**
2. Blocker #1 (Credentials)

**Затем что предотвращает крэши:**
3. Blocker #5 (try-catch)
4. Blocker #4 (error boundaries)

**Наконец UX:**
5. Blocker #3 (контакты)

---

**Успешного фикса! 🚀**

*После исправления всех блокеров повторить аудит и убедиться что `npm run build` успешен.*
