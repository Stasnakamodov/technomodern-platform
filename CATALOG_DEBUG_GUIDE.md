# 🔍 Руководство по отладке каталога

**Дата:** 2025-11-12
**Проблема:** Товары загружаются из Supabase, но не отображаются на странице

---

## ✅ Что мы проверили

### 1. База данных Supabase
- ✅ **459 товаров** в таблице `products`
- ✅ **8 поставщиков** в таблице `suppliers`
- ✅ **53 категории** в таблице `categories`
- ✅ Все изображения обновлены на Unsplash Collections

### 2. API доступ
- ✅ Service Role Key работает (скрипты)
- ✅ Anon Key работает (клиент)
- ✅ RLS политики не блокируют доступ

### 3. Тестовые скрипты
```bash
# Проверка загрузки данных
npx tsx scripts/test-catalog-load.ts
# ✅ Успешно: загружены 5 товаров

# Проверка RLS политик
npx tsx scripts/check-rls-policies.ts
# ✅ Успешно: доступ разрешен ко всем таблицам
```

---

## 🔧 Что было исправлено

### 1. Добавлено детальное логирование
**Файл:** `app/catalog/page.tsx`

Теперь в консоли браузера выводятся:
- 🔄 Начало загрузки
- 📦 Количество загруженных товаров
- 🏭 Количество поставщиков
- 📂 Количество категорий
- ✅ Результат трансформации данных
- ✅ Завершение загрузки

### 2. Улучшена обработка ошибок
- Проверка на `null` данных
- Проверка на пустые массивы
- Отдельное логирование каждого этапа

### 3. Добавлен отладочный UI
- Счетчик загруженных товаров в спиннере
- Информация о фильтрах в состоянии "Товары не найдены"

---

## 🚀 Как проверить сейчас

### Шаг 1: Запустите dev сервер
```bash
npm run dev
```

### Шаг 2: Откройте страницу каталога
```
http://localhost:3000/catalog
```

### Шаг 3: Откройте консоль браузера
**Chrome/Brave:**
- `Cmd + Option + I` (macOS)
- `F12` (Windows/Linux)

**Safari:**
- `Cmd + Option + C` (macOS)

### Шаг 4: Проверьте логи в консоли

Вы должны увидеть:
```
🔄 Начинаем загрузку товаров...
📦 Загружаем товары из Supabase...
📦 Ответ от Supabase (товары): {dataLength: 459, error: null, hasData: true}
🏭 Загружаем поставщиков...
🏭 Загружено поставщиков: 8
📂 Загружаем категории...
📂 Загружено категорий: 53
🔄 Трансформируем данные...
✅ Трансформировано 459 товаров
📦 Первый товар: {id: '...', name: '...', ...}
✅ Товары установлены в state
✅ Загрузка завершена, loading = false
```

---

## ❌ Возможные проблемы и решения

### Проблема 1: Ошибка CORS
**Симптом:** В консоли `Access-Control-Allow-Origin error`

**Решение:**
1. Проверьте что URL Supabase правильный в `lib/supabase.ts`
2. Убедитесь что используется `anon` ключ, а не `service_role`

### Проблема 2: Ошибка авторизации
**Симптом:** `Error 401: Unauthorized`

**Решение:**
1. Проверьте срок действия ключа в Supabase Dashboard
2. Settings → API → Project API keys
3. Скопируйте новый `anon public` ключ

### Проблема 3: Данные не загружаются
**Симптом:** `dataLength: 0` в консоли

**Решение:**
```bash
# Проверьте что данные есть в БД
npx tsx scripts/test-catalog-load.ts

# Если данных нет - импортируйте заново
npx tsx scripts/import-to-supabase.ts
```

### Проблема 4: Товары загружаются, но не отображаются
**Симптом:** Консоль показывает `✅ Трансформировано 459 товаров`, но на странице пусто

**Возможные причины:**
1. **Ошибка в ProductCard компоненте**
   - Откройте `app/catalog/components/ProductCard.tsx`
   - Проверьте что нет ошибок рендеринга

2. **Ошибка в CategoryBrowser**
   - Проверьте `app/catalog/components/CategoryBrowser.tsx`
   - Возможно компонент падает при рендеринге

3. **CSS конфликт**
   - Проверьте что Tailwind CSS загружен
   - Посмотрите во вкладке Elements - есть ли элементы на странице

### Проблема 5: React hydration error
**Симптом:** `Text content does not match server-rendered HTML`

**Решение:**
- Это нормально для dev режима при SSR
- В production этой ошибки не будет
- Можно игнорировать если товары отображаются

---

## 📊 Структура данных

### Product (после трансформации)
```typescript
{
  id: string              // UUID
  name: string            // Название товара
  price: number           // Цена в рублях
  description: string     // Описание
  images: string[]        // Массив URL изображений
  category: string        // Название категории
  inStock: boolean        // Наличие
  minOrder: number        // Минимальный заказ
  sku: string            // Артикул (prod-0001)
  supplier_name: string   // Название поставщика
}
```

### Пример товара из Supabase
```json
{
  "id": "000005ae-0000-0000-0000-000005ae0000",
  "name": "Крем для лица BeautyLux Натуральный",
  "price": 3321,
  "description": "Оригинальный крем для лица...",
  "images": ["https://source.unsplash.com/collection/3213364/600x400"],
  "category": "Красота и здоровье",
  "inStock": true,
  "minOrder": 123,
  "sku": "prod-0454",
  "supplier_name": "Shenzhen Electronics Ltd"
}
```

---

## 🛠 Команды для отладки

```bash
# Проверить что сервер запущен
curl http://localhost:3000/catalog

# Проверить доступ к Supabase API
curl 'https://rbngpxwamfkunktxjtqh.supabase.co/rest/v1/products?select=*&limit=1' \
  -H "apikey: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

# Проверить загрузку данных (серверный скрипт)
npx tsx scripts/test-catalog-load.ts

# Проверить RLS политики
npx tsx scripts/check-rls-policies.ts

# Переимпортировать данные (если нужно)
npx tsx scripts/import-to-supabase.ts
```

---

## 📸 Скриншоты для отладки

Пришлите скриншоты:

1. **Консоль браузера** (весь вывод с логами)
2. **Network tab** (запросы к Supabase)
3. **Elements tab** (структура HTML)
4. **Страница каталога** (что видите)

---

## 🔗 Полезные ссылки

- **Supabase Dashboard**: https://rbngpxwamfkunktxjtqh.supabase.co
- **Table Editor (Products)**: Dashboard → Table Editor → products
- **API Settings**: Dashboard → Settings → API
- **Logs**: Dashboard → Logs → Explorer

---

## 📝 Следующие шаги

После того как увидите логи в консоли:

1. **Если товары загружаются успешно:**
   - Проверьте компоненты `ProductCard` и `CategoryBrowser`
   - Возможна ошибка рендеринга

2. **Если товары не загружаются:**
   - Проверьте Network tab в браузере
   - Смотрите есть ли запросы к Supabase
   - Проверьте ответы от API

3. **Если есть ошибки в консоли:**
   - Скопируйте полный текст ошибки
   - Проверьте стек трейс
   - Найдите строку с ошибкой

---

**Статус:** ⏳ Ожидаем логи из консоли браузера
**Создано:** 2025-11-12
