# 🤖 Telegram Bot для приёма заявок - Руководство по настройке

**Дата:** 2025-11-17
**Проект:** ТехноМодерн
**Статус:** ✅ Готово к использованию

---

## 📋 Что было реализовано

### ✅ Созданные компоненты:

1. **Таблица `orders` в Supabase** - хранение всех заявок
2. **API endpoint** `/api/orders/submit` - обработка и отправка заявок
3. **Компонент формы** `OrderFormSection` - UI для приёма заявок
4. **Интеграция с Telegram** - автоматическая отправка уведомлений

---

## 🚀 Быстрая настройка (5 минут)

### Шаг 1: Применить миграцию в Supabase

1. Откройте Supabase Dashboard: https://supabase.com/dashboard
2. Перейдите в ваш проект `rbngpxwamfkunktxjtqh`
3. Нажмите **SQL Editor** (в левом меню)
4. Нажмите **New query**
5. Скопируйте содержимое файла:
   ```bash
   supabase/migrations/004_create_orders_table.sql
   ```
6. Вставьте в редактор и нажмите **Run**
7. ✅ Таблица `orders` создана!

### Шаг 2: Узнать свой Telegram Chat ID

**Вариант 1 - Через бота:**
1. Откройте Telegram
2. Найдите бота **@userinfobot**
3. Нажмите **Start**
4. Скопируйте ваш ID (например: `123456789`)

**Вариант 2 - Создать группу (рекомендуется для команды):**
1. Создайте группу в Telegram
2. Добавьте туда вашего бота (найдите его по username)
3. Напишите **@userinfobot** в группу
4. Скопируйте Chat ID группы (например: `-1001234567890`)

### Шаг 3: Настроить .env.local

Откройте файл `.env.local` и найдите эти строки:

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=8551072778:AAGdeTxdhZV1ESdzpsNLtTThY2sLecfoNos
# ID чата куда отправлять заявки (ваш Telegram ID или ID группы)
TELEGRAM_CHAT_ID=YOUR_CHAT_ID_HERE
```

**Замените `YOUR_CHAT_ID_HERE` на ваш реальный ID!**

Пример:
```env
TELEGRAM_CHAT_ID=123456789
```

Или для группы:
```env
TELEGRAM_CHAT_ID=-1001234567890
```

### Шаг 4: Перезапустить сервер

```bash
# Остановите dev сервер (Ctrl+C)
npm run dev
```

---

## 🧪 Тестирование

### 1. Проверка формы на сайте

1. Откройте http://localhost:3000
2. Прокрутите вниз до секции **"Оставьте заявку"**
3. Заполните форму:
   - **Имя:** Тест Тестов
   - **Телефон:** +7 999 123 45 67
   - **Email:** test@example.com (опционально)
   - **Название товара:** Тестовый товар
   - **Комментарий:** Это тестовая заявка

4. Нажмите **"Отправить заявку"**

### 2. Ожидаемый результат

✅ **На сайте:**
- Появится уведомление: "✅ Заявка отправлена!"
- Форма очистится

✅ **В Telegram:**
Вы получите красиво отформатированное сообщение:

```
🔔 НОВАЯ ЗАЯВКА #12345678

👤 Клиент:
├ Имя: Тест Тестов
├ Телефон: +7 999 123 45 67
└ Email: test@example.com

📦 Товар:
└ Название: Тестовый товар

💬 Сообщение:
Это тестовая заявка

⏰ Время: 17.11.2025, 22:30:45
🆔 ID заявки: 12345678-90ab-cdef-1234-567890abcdef
```

✅ **В Supabase:**
- Откройте **Table Editor** → **orders**
- Увидите новую запись с полями:
  - `customer_name`
  - `customer_phone`
  - `telegram_message_id` (ID сообщения в Telegram)
  - `status: "new"`

---

## 📊 Структура базы данных

### Таблица `orders`:

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | Уникальный ID заявки |
| `customer_name` | VARCHAR | Имя клиента ⭐ |
| `customer_phone` | VARCHAR | Телефон ⭐ |
| `customer_email` | VARCHAR | Email |
| `customer_telegram` | VARCHAR | Telegram username |
| `product_name` | VARCHAR | Название товара |
| `product_url` | TEXT | Ссылка на товар |
| `product_category` | VARCHAR | Категория |
| `quantity` | INTEGER | Количество (по умолчанию: 1) |
| `target_price` | DECIMAL | Целевая цена в рублях |
| `message` | TEXT | Комментарий клиента |
| `marketplace` | VARCHAR | Площадка (Alibaba, 1688 и т.д.) |
| `status` | VARCHAR | Статус: `new`, `processing`, `completed`, `cancelled` |
| `telegram_message_id` | INTEGER | ID сообщения в Telegram |
| `telegram_chat_id` | VARCHAR | ID чата Telegram |
| `source` | VARCHAR | Источник: `website`, `telegram_bot`, `whatsapp` |
| `created_at` | TIMESTAMPTZ | Дата создания |
| `updated_at` | TIMESTAMPTZ | Дата обновления |

⭐ = обязательное поле

---

## 🔧 Файлы проекта

### Новые файлы:

```
technomodern-platform/
├── app/
│   ├── api/
│   │   └── orders/
│   │       └── submit/
│   │           └── route.ts          # ← API endpoint для заявок
│   ├── layout.tsx                     # ← Обновлен (добавлен Toaster)
│   └── page.tsx                       # ← Обновлен (добавлена форма)
├── components/
│   └── order-form-section.tsx         # ← Форма заявки
├── supabase/
│   └── migrations/
│       └── 004_create_orders_table.sql # ← Миграция БД
├── .env.local                         # ← Обновлен (TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID)
├── .env.example                       # ← Обновлен (шаблон для Telegram)
└── TELEGRAM_BOT_SETUP_GUIDE.md        # ← Этот файл
```

---

## 🎨 Возможности формы

### Поля формы:

**Обязательные:**
- Имя клиента
- Телефон

**Опциональные:**
- Email
- Telegram username
- Название товара
- Ссылка на товар
- Площадка (Alibaba, 1688, Taobao)
- Количество
- Целевая цена
- Комментарий

### Особенности:

- ✅ Валидация полей
- ✅ Красивые уведомления (toast)
- ✅ Автоочистка формы после отправки
- ✅ Responsive дизайн (адаптивность)
- ✅ Индикатор загрузки

---

## 🔐 Безопасность

### Что реализовано:

1. **Переменные окружения** - токен бота хранится в `.env.local`
2. **Валидация на сервере** - проверка обязательных полей
3. **Логирование ошибок** - все ошибки записываются в консоль
4. **Graceful degradation** - если Telegram не отвечает, заявка всё равно сохраняется в БД

### Что НЕ делать:

❌ Не коммитьте `.env.local` в Git!
❌ Не публикуйте токен бота публично!
❌ Не используйте бот для спама!

---

## 📈 Следующие шаги (опционально)

### Расширение функционала:

1. **Статусы заявок**
   - Создать админ-панель для управления заявками
   - Менять статус: `new` → `processing` → `completed`

2. **Уведомления клиентам**
   - Отправлять SMS/Email при смене статуса
   - Telegram бот для общения с клиентами

3. **Аналитика**
   - Dashboard с количеством заявок
   - Графики конверсии
   - Топ площадок/категорий

4. **Интеграция с CRM**
   - Отправка заявок в Битрикс24, amoCRM и т.д.
   - Синхронизация статусов

5. **Автоответы в Telegram**
   - Команды для бота: `/status`, `/list`, `/complete`
   - Inline кнопки для быстрых действий

---

## 🐛 Решение проблем

### Проблема: Заявка отправляется, но не приходит в Telegram

**Решение:**
1. Проверьте `TELEGRAM_CHAT_ID` в `.env.local`
2. Убедитесь, что бот добавлен в группу (если используете группу)
3. Проверьте консоль браузера (F12) на ошибки
4. Проверьте консоль сервера (`npm run dev`) на ошибки

### Проблема: Ошибка "TELEGRAM_BOT_TOKEN не настроен"

**Решение:**
1. Проверьте `.env.local` - там должна быть строка:
   ```env
   TELEGRAM_BOT_TOKEN=8551072778:AAGdeTxdhZV1ESdzpsNLtTThY2sLecfoNos
   ```
2. Перезапустите dev сервер

### Проблема: Ошибка "Таблица orders не существует"

**Решение:**
1. Примените миграцию из `supabase/migrations/004_create_orders_table.sql`
2. В Supabase Dashboard → SQL Editor → Run

### Проблема: Форма не отправляется

**Решение:**
1. Откройте консоль браузера (F12)
2. Перейдите на вкладку **Network**
3. Попробуйте отправить форму
4. Найдите запрос `/api/orders/submit`
5. Посмотрите ответ сервера

---

## 📞 Поддержка

Если возникли вопросы:
1. Проверьте логи в консоли браузера (F12)
2. Проверьте логи сервера (`npm run dev`)
3. Проверьте Supabase Dashboard → Logs

---

## ✅ Чек-лист готовности

Перед деплоем проверьте:

- [ ] Миграция применена в Supabase
- [ ] `TELEGRAM_BOT_TOKEN` настроен в `.env.local`
- [ ] `TELEGRAM_CHAT_ID` настроен в `.env.local`
- [ ] Форма видна на сайте
- [ ] Тестовая заявка отправилась успешно
- [ ] Сообщение пришло в Telegram
- [ ] Заявка сохранилась в Supabase
- [ ] Переменные окружения добавлены в Vercel/VPS

---

## 🎉 Готово!

Ваш Telegram бот для приёма заявок настроен и работает!

**Что дальше?**
- Протестируйте с реальными данными
- Настройте уведомления для команды
- Добавьте аналитику заявок

Успехов! 🚀
