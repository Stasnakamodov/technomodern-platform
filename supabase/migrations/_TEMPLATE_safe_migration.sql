-- ============================================================
-- БЕЗОПАСНАЯ МИГРАЦИЯ ДЛЯ ТЕХНОМОДЕРА
-- ============================================================
--
-- ВНИМАНИЕ: Эта база данных ОБЩАЯ для двух проектов!
--
-- Техномодер    → схема public   (НАША)
-- Козырный      → схема kozyrny  (НЕ ТРОГАТЬ!)
--
-- ПРАВИЛА:
-- 1. ВСЕГДА указывай схему явно: public.table_name
-- 2. НИКОГДА не используй DROP/ALTER без схемы
-- 3. НИКОГДА не трогай схему kozyrny
-- ============================================================

-- Пример создания таблицы:
-- CREATE TABLE public.new_table (
--     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
--     name TEXT NOT NULL,
--     created_at TIMESTAMPTZ DEFAULT NOW()
-- );

-- Пример изменения таблицы:
-- ALTER TABLE public.products ADD COLUMN new_field TEXT;

-- Пример удаления (ОСТОРОЖНО!):
-- DROP TABLE public.old_table;

-- ============================================================
-- ТВОЙ КОД НИЖЕ:
-- ============================================================


