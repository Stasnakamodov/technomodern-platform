# 📘 Руководство по обновлению деплоя

## 🎯 Что было обновлено

**Дата:** 2025-11-16
**Изменения:**
- ✅ Обновлены все 69 изображений в категории "Электроника"
- ✅ Исправлено нерелевантное изображение "Наушники Baseus Max"
- ✅ Все изображения уникальны (дубликатов нет)
- ✅ Изменения запушены в GitHub

**Коммиты:**
```
9534c5a - fix: Исправить нерелевантное изображение "Наушники Baseus Max"
1566850 - feat: Полностью обновить все изображения в категории "Электроника"
```

---

## 📱 О мобильной версии

**Вопрос:** Есть ли отдельный модуль для мобильной версии?

**Ответ:** НЕТ. Проект использует **адаптивный дизайн** (responsive design).

**Что это значит:**
- Одно приложение работает на всех устройствах (десктоп, планшет, мобильный)
- Используется Tailwind CSS с responsive breakpoints (`md:`, `lg:`, `max-md:`)
- Автоматическая адаптация интерфейса под размер экрана
- Все обновления применяются ко всем версиям сразу

**Проверено:**
- ✅ Layout имеет viewport настройки для мобильных
- ✅ Компоненты используют адаптивные классы
- ✅ Карточки товаров корректно отображаются на мобильных

---

## 🚀 Как обновить деплой на VPS

### ⚡ Способ 1: Автоматический (Рекомендуется)

Используйте готовый скрипт:

```bash
# Запустите скрипт деплоя
./deploy-to-vps.sh
```

Скрипт автоматически:
1. Подключится к VPS серверу
2. Обновит код из GitHub
3. Пересоберет проект
4. Перезапустит PM2
5. Покажет статус

---

### 🔧 Способ 2: Вручную

#### Шаг 1: Подключитесь к серверу

```bash
ssh root@155.212.164.197
```

#### Шаг 2: Перейдите в директорию проекта

```bash
cd /var/www/technomodern-platform
```

#### Шаг 3: Обновите код

```bash
git pull
```

**Ожидаемый результат:**
```
Updating 6379166..9534c5a
Fast-forward
 scripts/fix-baseus-max-image.ts          | 116 +++++++++++++
 scripts/update-electronics-smart.ts      | 180 +++++++++++++++++++
 scripts/check-electronics-duplicates.ts  |  61 +++++++
 ...
```

#### Шаг 4: Установите зависимости (если нужно)

```bash
npm install --legacy-peer-deps
```

**Примечание:** Выполняйте только если добавлены новые пакеты. В данном случае можно пропустить.

#### Шаг 5: Пересоберите проект

```bash
npm run build
```

**Время сборки:** ~2-3 минуты

**Ожидаемый результат:**
```
✓ Compiled successfully
✓ Collecting page data
✓ Generating static pages
✓ Finalizing page optimization
```

#### Шаг 6: Перезапустите PM2

```bash
pm2 restart technomodern
```

**Ожидаемый результат:**
```
┌────┬──────────────┬─────────┬─────────┬─────────┐
│ id │ name         │ mode    │ pid     │ status  │
├────┼──────────────┼─────────┼─────────┼─────────┤
│ 0  │ technomodern │ fork    │ 15234   │ online  │
└────┴──────────────┴─────────┴─────────┴─────────┘
```

#### Шаг 7: Проверьте статус

```bash
pm2 status
```

---

## ✅ Проверка обновления

### 1. Проверьте что сайт работает

```bash
curl http://155.212.164.197
```

### 2. Откройте в браузере

Перейдите на: **http://155.212.164.197/catalog**

### 3. Проверьте категорию "Электроника"

В каталоге:
1. Выберите категорию "Электроника" (💻)
2. Проверьте что изображения товаров релевантны:
   - Смартфоны → фото телефонов
   - Ноутбуки → фото ноутбуков
   - Наушники → фото наушников
3. Проверьте что все изображения загружаются
4. Проверьте что нет дубликатов

### 4. Проверьте товар "Наушники Baseus Max"

Найдите товар "Наушники Baseus Max" (SKU: prod-0046):
- ✅ Должно быть изображение беспроводных наушников
- ❌ НЕ должно быть изображение слухового аппарата

### 5. Проверьте мобильную версию

На телефоне или с помощью DevTools:
1. Откройте http://155.212.164.197/catalog
2. Переключите на мобильный вид (375px ширина)
3. Проверьте что каталог корректно отображается
4. Проверьте что карточки товаров адаптированы

---

## 📊 Полезные команды

### Просмотр логов приложения
```bash
ssh root@155.212.164.197 "pm2 logs technomodern"
```

### Просмотр логов в реальном времени
```bash
ssh root@155.212.164.197 "pm2 logs technomodern --lines 100"
```

### Проверка статуса
```bash
ssh root@155.212.164.197 "pm2 status"
```

### Перезапуск приложения
```bash
ssh root@155.212.164.197 "pm2 restart technomodern"
```

### Остановка приложения
```bash
ssh root@155.212.164.197 "pm2 stop technomodern"
```

### Проверка использования ресурсов
```bash
ssh root@155.212.164.197 "pm2 monit"
```

---

## 🔍 Troubleshooting

### Проблема: Git pull не работает

**Решение:**
```bash
# Проверьте статус git
git status

# Сбросьте локальные изменения (если нужно)
git reset --hard origin/main

# Попробуйте снова
git pull
```

### Проблема: Build завершается с ошибкой

**Решение:**
```bash
# Очистите кэш
rm -rf .next
rm -rf node_modules

# Переустановите зависимости
npm install --legacy-peer-deps

# Попробуйте снова
npm run build
```

### Проблема: PM2 не перезапускается

**Решение:**
```bash
# Остановите приложение
pm2 stop technomodern

# Запустите заново
pm2 start npm --name technomodern -- start

# Сохраните конфигурацию
pm2 save
```

### Проблема: Старые изображения все еще отображаются

**Решение:**
```bash
# Очистите кэш браузера
# В Chrome/Firefox: Ctrl+Shift+R (или Cmd+Shift+R на Mac)

# Или очистите кэш Next.js на сервере
rm -rf .next/cache
npm run build
pm2 restart technomodern
```

---

## 📈 Что дальше?

### Рекомендуемые действия:

1. **Настроить HTTPS** (если еще не настроено)
   ```bash
   apt install -y certbot python3-certbot-nginx
   certbot --nginx -d techno-modern.ru
   ```

2. **Настроить автоматические деплои**
   - GitHub Actions для CI/CD
   - Webhook для автоматического обновления

3. **Настроить мониторинг**
   - PM2 Plus для продвинутого мониторинга
   - Настройка алертов

4. **Создать бэкапы**
   - Автоматические бэкапы базы данных Supabase
   - Бэкапы кода

---

## 🎊 Готово!

После успешного обновления:

✅ Все 69 товаров категории "Электроника" имеют релевантные изображения
✅ Все изображения уникальны
✅ Мобильная версия работает корректно
✅ Деплой обновлен на production сервере

**Сайт:** http://155.212.164.197
**Каталог:** http://155.212.164.197/catalog

---

**Создано:** Claude Code
**Дата:** 2025-11-16
**Версия:** 1.0
