# 🔧 Промпт для нового AI-ассистента

## Контекст задачи

Я работаю над Next.js приложением (версия 16.0.0 с Turbopack). Проект - это платформа для работы с китайскими поставщиками товаров.

**Проблема:** Товары в каталоге НЕ ОТОБРАЖАЮТСЯ на странице, хотя:
- ✅ Данные загружаются из Supabase успешно (425 товаров)
- ✅ В консоли видны все логи о загрузке
- ✅ `loading = false` устанавливается корректно
- ✅ Товары трансформируются и устанавливаются в state
- ✅ RLS политики настроены, доступ разрешён

**Но товары просто НЕ ВИДНЫ на странице!**

---

## 🔍 Текущее состояние

### Что работает:
1. **База данных Supabase:**
   - URL: `https://rbngpxwamfkunktxjtqh.supabase.co`
   - 425 товаров (фильтр `in_stock = true`)
   - 8 поставщиков
   - 53 категории
   - Все изображения из Unsplash Collections

2. **Загрузка данных:**
   ```
   🔄 Начинаем загрузку товаров...
   📦 Ответ от Supabase: {dataLength: 425, error: null, hasData: true}
   🏭 Загружено поставщиков: 8
   📂 Загружено категорий: 53
   ✅ Трансформировано 425 товаров
   ✅ Загрузка завершена, loading = false
   ```

3. **Логи рендеринга:**
   - CategoryBrowser рендерится
   - dynamicCategories строятся (8 категорий)
   - filteredProducts содержит 425 товаров

### Что НЕ работает:
- ❌ Товары не видны на странице визуально
- ❌ Карточки товаров не отображаются
- ❌ Пользователь видит пустую страницу или только загрузку

---

## 📁 Ключевые файлы

### 1. `/app/catalog/page.tsx` (главная страница каталога)
```typescript
'use client'

// Основной компонент - загружает товары из Supabase
// useEffect загружает products, suppliers, categories
// Трансформирует данные в формат Product[]
// Рендерит сетку с ProductCard компонентами
```

**Проблема может быть здесь:**
- Проверь условия рендеринга (loading ? ... : filteredProducts.length === 0 ? ... : ...)
- Возможно проблема в логике filteredProducts
- Может быть CSS скрывает элементы

### 2. `/app/catalog/components/ProductCard.tsx` (карточка товара)
```typescript
// Упрощённая версия БЕЗ Framer Motion
// Использует обычный div с hover:-translate-y-1
// Фиксированная высота h-64 для изображения
// Цена в российском формате: toLocaleString('ru-RU')
```

**Уже исправлено:**
- ✅ Убран Framer Motion (был конфликт с SSR)
- ✅ Заменён aspect-[1.618/1] на h-64
- ✅ Упрощены размеры шрифтов
- ✅ Добавлена проверка на null/undefined

**Может быть проблема:**
- Карточка рендерится но не видна
- CSS проблема
- z-index или opacity

### 3. `/app/catalog/components/CategoryBrowser.tsx` (боковая панель)
```typescript
// Динамически строит категории из products
// useMemo для оптимизации
// Проверка: if (!products || products.length === 0) return []
```

**Уже исправлено:**
- ✅ Динамические категории вместо моков
- ✅ Логи убраны (кроме критичных ошибок)

### 4. `/lib/supabase.ts` (клиент Supabase)
```typescript
const supabaseUrl = 'https://rbngpxwamfkunktxjtqh.supabase.co'
const supabaseAnonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...'
export const supabase = createClient(supabaseUrl, supabaseAnonKey)
```

---

## 🐛 История попыток исправления

### Попытка 1: Добавили детальное логирование
**Результат:** Логи показывают что всё загружается, но товары не видны

### Попытка 2: Убрали Framer Motion
**Результат:** Убрали `motion.div`, заменили на обычный div

### Попытка 3: Упростили ProductCard
**Результат:** Уменьшили размеры, убрали aspect-ratio

### Попытка 4: Создали SimpleProductCard (розовые карточки)
**Результат:** ✅ РОЗОВЫЕ КАРТОЧКИ ВИДНЫ! Это доказывает что:
- ✅ Данные загружаются
- ✅ Рендеринг работает
- ✅ Grid работает
- ❌ Проблема в ProductCard компоненте

### Попытка 5: Упростили ProductCard (текущая версия)
**Результат:** ❌ Всё ещё не видны товары

---

## 🎯 Что нужно сделать

### Задача: Найти почему ProductCard не отображается

**Возможные причины:**

1. **CSS проблема:**
   - `display: none` где-то в стилях
   - `opacity: 0`
   - `visibility: hidden`
   - Проблема с z-index
   - Элементы за пределами viewport

2. **Проблема с Card компонентом:**
   - `/components/ui/card.tsx` может иметь проблемные стили
   - Проверь базовые компоненты shadcn/ui

3. **Проблема с данными:**
   - Какое-то поле в product undefined/null
   - Ошибка в рендеринге, которая не логируется
   - React падает молча

4. **Проблема с Grid:**
   - `grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-8` не работает
   - Проблема с Tailwind CSS

---

## 📋 План действий для нового AI

### Шаг 1: Диагностика
```typescript
// В /app/catalog/page.tsx после loadProducts:
console.log('ПРОВЕРКА:', {
  productsLoaded: products.length,
  filteredLength: filteredProducts.length,
  loading: loading,
  firstProduct: products[0]
})
```

### Шаг 2: Проверь рендеринг ProductCard
```typescript
// В ProductCard добавь:
console.log('ProductCard RENDER:', {
  name: product.name,
  hasImages: product.images?.length > 0,
  price: product.price
})

// Или добавь видимый индикатор:
return (
  <div style={{ border: '5px solid red', minHeight: '200px', backgroundColor: 'yellow' }}>
    <p style={{ fontSize: '24px', color: 'black' }}>ТЕСТ: {product.name}</p>
  </div>
)
```

### Шаг 3: Проверь что SimpleProductCard работает
```typescript
// Временно замени ProductCard на SimpleProductCard:
import SimpleProductCard from './components/SimpleProductCard'

// В map:
<SimpleProductCard key={product.id} product={product} />
```

**Если SimpleProductCard видна, а ProductCard нет** → проблема в ProductCard

### Шаг 4: Проверь CSS
```typescript
// Открой DevTools → Elements
// Найди div.grid.grid-cols-1
// Проверь есть ли внутри элементы
// Проверь computed styles карточек
```

### Шаг 5: Бинарный поиск проблемы
```typescript
// Постепенно упрощай ProductCard, удаляя части:
// 1. Убери всё кроме названия
// 2. Если видно - добавляй по одному элементу
// 3. Найди что ломает рендеринг
```

---

## 🔧 Быстрые команды

### Запуск проекта:
```bash
cd /Users/user/Downloads/code
npm run dev
```

### Открыть каталог:
```
http://localhost:3000/catalog
```

### Тестовая страница (работает):
```
http://localhost:3000/catalog-test
```

### Проверить данные в Supabase:
```bash
npx tsx scripts/test-catalog-load.ts
```

---

## 📊 Консоль браузера (последний вывод)

```
✅ Трансформировано 425 товаров
✅ Товары установлены в state
✅ Загрузка завершена, loading = false
📂 Динамические категории построены: 8 категорий
📂 CategoryBrowser render: {productsCount: 425, categoriesCount: 8}
```

**Всё загружается! Но товары НЕ ВИДНЫ!**

---

## 💡 Подсказки

### Что точно работает:
- ✅ Supabase подключение
- ✅ Загрузка данных
- ✅ Трансформация данных
- ✅ React state updates
- ✅ SimpleProductCard отображается (розовые карточки)

### Где искать проблему:
1. **ProductCard.tsx** - что-то в компоненте ломает рендеринг
2. **Card компонент** - `/components/ui/card.tsx` может иметь проблемные стили
3. **CSS конфликт** - возможно какой-то global CSS скрывает элементы
4. **Условие рендеринга** - проверь `loading ? ... : filteredProducts.length === 0 ? ... : ...`

---

## 🎯 Цель

**Сделать так чтобы товары были ВИДНЫ на странице!**

Пользователь должен увидеть сетку из 425 карточек товаров с:
- Изображениями
- Названиями
- Ценами
- Кнопками

---

## 📝 Важные детали

### Структура Product:
```typescript
interface Product {
  id: string              // UUID
  name: string           // Название
  price: number          // Цена в рублях
  description: string    // Описание
  images: string[]       // URL изображений
  category: string       // Название категории
  inStock: boolean       // Наличие
  minOrder: number       // MOQ
  sku: string           // Артикул
  supplier_name: string  // Поставщик
}
```

### SimpleProductCard (РАБОТАЕТ):
```typescript
<div style={{
  border: '2px solid #e11d48',
  padding: '16px',
  backgroundColor: '#fef2f2',
  borderRadius: '8px',
  minHeight: '200px'
}}>
  <h3>{product.name}</h3>
  <p>{product.price.toLocaleString('ru-RU')} ₽</p>
</div>
```

**Эта карточка ВИДНА! Значит проблема в ProductCard!**

---

## 🚨 СРОЧНО

Нужно найти что именно в ProductCard ломает отображение и исправить это!

**Подход:**
1. Сравни SimpleProductCard (работает) и ProductCard (не работает)
2. Найди разницу
3. Постепенно добавляй элементы ProductCard в SimpleProductCard
4. Найди что ломает рендеринг
5. Исправь

---

## 📞 Контекст проекта

- **Framework:** Next.js 16.0.0 (App Router, Turbopack)
- **База данных:** Supabase (PostgreSQL)
- **UI:** shadcn/ui + Tailwind CSS
- **TypeScript:** Да
- **Стиль:** Client components ('use client')

---

## ✅ Критерий успеха

Пользователь открывает `/catalog` и видит:
- Сетку карточек товаров (3 колонки на десктопе)
- Каждая карточка с изображением, названием, ценой
- Боковую панель с категориями
- Рабочий поиск и фильтры

**Сейчас он видит пустую страницу или только загрузку!**

---

Помоги исправить ProductCard чтобы товары стали видны!
